name: Review Bot Hello (Reusable)

on:
  # 호출되는 워크플로우
  workflow_call:
    inputs:
      mention:
        type: string
        required: false
        default: "@review-bot"
      reply:
        type: string
        required: false
        default: "안녕하세요 리뷰봇입니다. 🤖 (호출 확인 완료!)"

permissions:
  issues: write
  pull-requests: read

jobs:
  hello-reply:
    if: github.event.issue.pull_request && contains(github.event.comment.body, inputs.mention)
    runs-on: ubuntu-latest
    steps:
      - name: Reply to PR
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.issue.number }}
        run: |
          gh issue comment "$PR_NUMBER" --body "${{ inputs.reply }}"
